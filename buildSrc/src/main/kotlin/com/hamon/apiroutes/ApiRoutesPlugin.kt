package com.hamon.apiroutes

import org.gradle.api.Plugin
import org.gradle.api.Project

// T3 + T6 — wires extension and task
class ApiRoutesPlugin : Plugin<Project> {
    override fun apply(project: Project) = TODO("T3 + T6")
}
